const Profile = (props) => {
    return (
        <>
        <h2>
            Profile Functional Component
        </h2>
        <h3>Name: {props.name}</h3>
        </>
    );
};

export default Profile;